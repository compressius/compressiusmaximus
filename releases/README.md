# Release records

Each version folder contains only the release manifest and SHA-256 checksums.
The binary archives are attached to the matching GitHub release and mirrored
separately on `cmx.llm.kiwi`; no core source is stored here.
